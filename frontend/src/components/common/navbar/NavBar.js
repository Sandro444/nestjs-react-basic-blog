import { NavBarWrapper } from "./components";
import NavBarItems from "./NavBarItems";
const NavBar = () => {
  return (
    <NavBarWrapper>
      <NavBarItems />
    </NavBarWrapper>
  );
};

export default NavBar;
